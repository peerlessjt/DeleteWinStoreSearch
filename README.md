I do not want any Microsoft Store results showing up in the Start menu search.
Even with every single reg key set for disabling online search and other related keys, it was still happening.

This script rips the SearchProducts straight out of the Store DB.

Note: I believe this has since been fixed, the script should not be required on newer versions of Windows.
